* Rename all EC_XXX to ec_xx

* Change git branch prompt color to light blue

* Put squalre breakets inside sed expression in git branch prompt

* Do something with systems where ${INPUTRC} defined and points to /etc/inputrc
